$output.file("src/app/entities/${entity.model.type.toLowerCase()}/${entity.model.type.toLowerCase()}-autocomplete/${entity.model.type.toLowerCase()}-autocomplete.component.spec.ts")##
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ${entity.model.type}AutocompleteComponent } from './${entity.model.type.toLowerCase()}-autocomplete.component';

describe('${entity.model.type}AutocompleteComponent', () => {
  let component: ${entity.model.type}AutocompleteComponent;
  let fixture: ComponentFixture<${entity.model.type}AutocompleteComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ${entity.model.type}AutocompleteComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(${entity.model.type}AutocompleteComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
