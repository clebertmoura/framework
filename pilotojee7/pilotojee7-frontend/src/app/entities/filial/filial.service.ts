//
// Source code generated by Celerio, a Jaxio product.
// Documentation: http://www.jaxio.com/documentation/celerio/
// Follow us on twitter: @jaxiosoft
// Need commercial support ? Contact us: info@jaxio.com
// Template celerio-pack-frontend:src/app/entities/entity/entity.service.ts.e.vm
// Template is part of Open Source Project: https://github.com/jaxio/javaee-lab
//
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { MessageService } from '../../util/message.service';
import { EntityService } from '../../framework/service/entity.service';
import { Observable, forkJoin } from 'rxjs';
import { Filial } from './filial';

@Injectable()
export class FilialService extends EntityService<Filial> {

  constructor(protected http: HttpClient,protected messageService: MessageService) {
    super(http, messageService, 'Filial');
  }

  public newEntity(response?: any): Filial {
    return new Filial(response, 1);
  }

  public newEntityList(response: any): Filial[] {
    return Filial.toArray(response, 1);
  }

  protected flatMapEntityRelations(entity: Filial): Observable<any[]> {
    const observables: Observable<any>[] = [];

    if (observables.length > 0) {
      return forkJoin(observables);
    } else {
      return ;
    }
  }

  protected mapEntityRelations(entity: Filial, response: any): Filial {
    return entity;
  }
  
}
